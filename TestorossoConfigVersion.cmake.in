set(PACKAGE_VERSION "@TESTOROSSO_VERSION@")

# Check wether the requested PACKAGE_FIND_VERSION is compatible
if("${PACKAGE_VERSION" VERSION_LESS "${PACKAGE_FIND_VERSION}")
    set(PACKAGE_VERSION_COMPATIBLE FALSE)
else()
    set(PACKAGE_VERSION_COMPATIBLE TRUE)
    if ("${PACKAGE_VERSION}" VERSION_REQUAL "${PACKAGE_FIND_VERSION}")
        set(PACKAGE_VERSION_EXACT TRUE)
    endif()
endif()